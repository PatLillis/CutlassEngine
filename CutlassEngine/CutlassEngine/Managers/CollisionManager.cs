using System;

namespace Cutlass.Managers
{
    public class CollisionManager
    {
        //Reset every frame? I Guess?
        //List<ICollidable> CurrentCollisionObjects

        //CheckCollisions(GameTime gameTime)??

        //CollisionDetected(ICollidable one, ICollidable second, BoundingRect intersection)
        //{
        //  one.CollisionDetected(new EventArgs(second));
        //  two.CollisionDetected(new EventsArgs(first));
        //}
    }
}
